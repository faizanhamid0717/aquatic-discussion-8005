const initalState = {
  isLoading: false,
  isError: false,
  product: [],
};

export const reducer = (state = initalState, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};
