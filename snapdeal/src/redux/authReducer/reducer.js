const initalState = {
  isLoading: false,
  isError: false,
  token: "",
  isAuth: false,
};

export const reducer = (state = initalState, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};
